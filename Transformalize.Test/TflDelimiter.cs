namespace Transformalize.Test {
    public class TflDelimiter : TflNode {
        public TflDelimiter() {
            Key("character");
            Property("name", string.Empty, true);
        }
    }
}