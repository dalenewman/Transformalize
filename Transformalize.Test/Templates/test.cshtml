@using System
@using Transformalize.Main
@{
    var process = (Process) Model.Process;
}

The answer is @(Model.Settings.Answer).
The process is @(process.Name).
The entities are:
@foreach (var entity in process.Entities)
{
    @(entity.Name + Environment.NewLine)
}