@functions {

    private string Double(object input)
    {
        string i = input.ToString();
        if (i == string.Empty)
        {
            return string.Empty;
        }

        return i + i;
    }

}